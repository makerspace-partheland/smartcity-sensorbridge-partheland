name: CI

on:
  push:
    branches: [ main ]
  pull_request_target:
    branches: [ main ]

permissions:
  contents: read

jobs:
  test:
    # Verhindere Loop: Nicht bei push von github-actions[bot]
    if: github.actor != 'github-actions[bot]' || github.event_name == 'pull_request_target'
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Python einrichten
        uses: actions/setup-python@v6
        with:
          python-version: '3.13.2'

      - name: Test- und Lint-Abhängigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements_test.txt ]; then
            pip install -r requirements_test.txt
          else
            echo "requirements_test.txt nicht gefunden – installiere Minimal-Set"
            pip install flake8 pytest pytest-asyncio pytest-cov pytest-mock
          fi

      - name: Lint (flake8)
        run: |
          flake8 . || echo "Lint-Warnungen gefunden (E/W werden derzeit nicht geblockt)"

      - name: Tests ausführen
        run: |
          set +e
          pytest -q --maxfail=1 --disable-warnings
          code=$?
          if [ "$code" -eq 5 ]; then
            echo "Keine Tests gefunden – CI OK"
            exit 0
          fi
          exit $code



